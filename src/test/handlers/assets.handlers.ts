/**
 * Assets handlers: subgraph mocks (Land Manager + Marketplace).
 * Worlds Content Server /wallet/contribute is in worlds-content-server.handlers.
 */
import { subgraphsHandlers } from './subgraphs.handlers'

const assetsHandlers = [...subgraphsHandlers]

export { assetsHandlers }
